<Window x:Class="Presentation.ReviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đánh giá Tour" Height="380" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F8FAFC">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Viết đánh giá của bạn" FontSize="18" FontWeight="Bold" Foreground="#1E3A5F" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="Đánh giá (Sao): " FontSize="14" Foreground="#374151" VerticalAlignment="Center"/>
            <ComboBox x:Name="cboRating" Width="80" Height="28" FontSize="14">
                <ComboBoxItem Content="5 ⭐" IsSelected="True"/>
                <ComboBoxItem Content="4 ⭐"/>
                <ComboBoxItem Content="3 ⭐"/>
                <ComboBoxItem Content="2 ⭐"/>
                <ComboBoxItem Content="1 ⭐"/>
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Bình luận:" FontSize="14" Foreground="#374151" Margin="0,0,0,8"/>
            <TextBox x:Name="txtComment" Height="100" Padding="8" FontSize="13" BorderBrush="#D1D5DB" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Hủy" Width="80" Height="36" Margin="0,0,10,0" Background="#E5E7EB" BorderThickness="0" FontSize="13" Cursor="Hand" Click="btnCancel_Click"/>
            <Button Content="Gửi đánh giá" Width="100" Height="36" Background="#F59E0B" Foreground="White" BorderThickness="0" FontSize="13" FontWeight="SemiBold" Cursor="Hand" Click="btnSubmit_Click"/>
        </StackPanel>
    </Grid>
</Window>
