<Page x:Class="Presentation.MyBookingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Booking của tôi" Background="#F8FAFC">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Booking của tôi" FontSize="22" FontWeight="Bold"
                   Foreground="#064E3B" Margin="0,0,0,16"/>

        <DataGrid CanUserReorderColumns="False" Grid.Row="1" x:Name="dgMyBookings" AutoGenerateColumns="False"
                  IsReadOnly="True" SelectionMode="Single" Background="White"
                  BorderBrush="#E5E7EB" GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F9FAFB" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding BookingId}" Width="60"/>
                <DataGridTextColumn Header="Tour" Binding="{Binding Schedule.Tour.TourName}" Width="200"/>
                <DataGridTextColumn Header="Ngày đi" Binding="{Binding Schedule.DepartureDate}" Width="110"/>
                <DataGridTextColumn Header="Ngày về" Binding="{Binding Schedule.ReturnDate}" Width="110"/>
                <DataGridTextColumn Header="Số người" Binding="{Binding NumberOfPeople}" Width="90"/>
                <DataGridTextColumn Header="Tổng tiền" Binding="{Binding TotalPrice, StringFormat={}{0:N0} VNĐ}" Width="140"/>
                <DataGridTextColumn Header="Ngày đặt" Binding="{Binding BookingDate}" Width="110"/>
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="110"/>
                <DataGridTemplateColumn Header="Thao tác" Width="270">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Chi tiết" Height="26" Width="55" Margin="0,0,5,0"
                                        Background="#3B82F6" Foreground="White" BorderThickness="0"
                                        FontSize="11" Cursor="Hand" Click="btnDetail_Click" Tag="{Binding BookingId}"/>
                                
                                <Button Content="Thanh toán" Height="26" Width="70" Margin="0,0,5,0"
                                        Background="#10B981" Foreground="White" BorderThickness="0"
                                        FontSize="11" Cursor="Hand" Click="btnPay_Click" Tag="{Binding}">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding Status}" Value="Confirmed" />
                                                        <Condition Binding="{Binding IsPaid}" Value="False" />
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </MultiDataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                
                                <Button Content="Đánh giá" Height="26" Width="60" Margin="0,0,5,0"
                                        Background="#F59E0B" Foreground="White" BorderThickness="0"
                                        FontSize="11" Cursor="Hand" Click="btnReview_Click" Tag="{Binding}">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                
                                <Button Content="Hủy" Height="26" Width="50"
                                        Background="#EF4444" Foreground="White" BorderThickness="0"
                                        FontSize="11" Cursor="Hand" Click="btnCancel_Click" Tag="{Binding BookingId}">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
