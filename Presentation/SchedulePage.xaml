<Page x:Class="Presentation.SchedulePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Lịch trình" Background="#F8FAFC">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Quản lý Lịch trình" FontSize="22" FontWeight="Bold"
                   Foreground="#1E3A5F" Margin="0,0,0,16"/>

        <!-- Toolbar: lọc Tour + Thêm + Hủy lịch -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Lọc theo Tour:" FontSize="13"
                       VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="cboFilterTour" Grid.Column="1" Height="36" FontSize="13"
                      BorderBrush="#D1D5DB" DisplayMemberPath="TourName"
                      SelectedValuePath="TourId" SelectionChanged="cboFilterTour_Changed"/>
            <Button Grid.Column="3" Content="+ Thêm lịch trình" Height="36" Padding="14,0"
                    Background="#2563EB" Foreground="White" BorderThickness="0"
                    FontSize="13" FontWeight="SemiBold" Cursor="Hand" Click="btnAdd_Click"
                    Margin="0,0,8,0"/>
        </Grid>

        <DataGrid Grid.Row="2" x:Name="dgSchedule" AutoGenerateColumns="False"
                  IsReadOnly="True" SelectionMode="Single" Background="White"
                  BorderBrush="#E5E7EB" GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F9FAFB" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ScheduleId}" Width="60"/>
                <DataGridTextColumn Header="Tour" Binding="{Binding Tour.TourName}" Width="180"/>
                <DataGridTextColumn Header="Ngày đi" Binding="{Binding DepartureDate}" Width="110"/>
                <DataGridTextColumn Header="Ngày về" Binding="{Binding ReturnDate}" Width="110"/>
                <DataGridTextColumn Header="Chỗ trống" Binding="{Binding AvailableSlot}" Width="90"/>
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="100"/>
                <DataGridTemplateColumn Header="Thao tác" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Sửa" Width="55" Height="26" Margin="0,0,4,0"
                                        Background="#F59E0B" Foreground="White" BorderThickness="0"
                                        FontSize="12" Cursor="Hand" Click="btnEdit_Click" Tag="{Binding ScheduleId}"/>
                                <Button Content="Hủy lịch" Width="65" Height="26" Margin="0,0,4,0"
                                        Background="#EF4444" Foreground="White" BorderThickness="0"
                                        FontSize="12" Cursor="Hand" Click="btnCancel_Click" Tag="{Binding ScheduleId}"/>
                                <Button Content="Xóa" Width="50" Height="26"
                                        Background="#6B7280" Foreground="White" BorderThickness="0"
                                        FontSize="12" Cursor="Hand" Click="btnDelete_Click" Tag="{Binding ScheduleId}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
