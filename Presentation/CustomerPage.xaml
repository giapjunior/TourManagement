<Page x:Class="Presentation.CustomerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Khách hàng" Background="#F8FAFC">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Quản lý Khách hàng" FontSize="22" FontWeight="Bold"
                   Foreground="#1E3A5F" Margin="0,0,0,16"/>

        <!-- Toolbar: Search -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtSearch" Grid.Column="0" Height="36" Padding="10,6"
                     FontSize="13" BorderBrush="#D1D5DB" Margin="0,0,8,0"
                     KeyUp="txtSearch_KeyUp"/>
            <Button Grid.Column="1" Content="🔍 Tìm kiếm" Height="36" Padding="14,0"
                    Background="#6B7280" Foreground="White" BorderThickness="0"
                    FontSize="13" Cursor="Hand" Click="btnSearch_Click"/>
        </Grid>

        <DataGrid CanUserReorderColumns="False" Grid.Row="2" x:Name="dgCustomer" AutoGenerateColumns="False"
                  IsReadOnly="True" SelectionMode="Single" Background="White"
                  BorderBrush="#E5E7EB" GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F9FAFB" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CustomerId}" Width="50"/>
                <DataGridTextColumn Header="Họ tên" Binding="{Binding FullName}" Width="160"/>
                <DataGridTextColumn Header="Điện thoại" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                <DataGridTextColumn Header="Địa chỉ" Binding="{Binding Address}" Width="150"/>
                <DataGridTemplateColumn Header="Trạng thái TK" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Account.IsActive}" Value="True">
                                                <Setter Property="Text" Value="Hoạt động"/>
                                                <Setter Property="Foreground" Value="#10B981"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Account.IsActive}" Value="False">
                                                <Setter Property="Text" Value="Đã khóa"/>
                                                <Setter Property="Foreground" Value="#EF4444"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Thao tác" Width="180">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Chi tiết" Width="60" Height="26" Margin="0,0,4,0"
                                        Background="#6366F1" Foreground="White" BorderThickness="0"
                                        FontSize="12" Cursor="Hand" Click="btnDetail_Click" Tag="{Binding CustomerId}"/>
                                <Button Content="Khóa/Mở" Width="65" Height="26"
                                        Background="#F59E0B" Foreground="White" BorderThickness="0"
                                        FontSize="12" Cursor="Hand" Click="btnToggle_Click" Tag="{Binding CustomerId}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
