<Page x:Class="Presentation.AccountPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Tài khoản" Background="#F8FAFC">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Quản lý Tài khoản" FontSize="22" FontWeight="Bold"
                   Foreground="#1E3A5F" Margin="0,0,0,16"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="+ Thêm tài khoản" Height="36" Padding="14,0"
                    Background="#2563EB" Foreground="White" BorderThickness="0"
                    FontSize="13" FontWeight="SemiBold" Cursor="Hand" Click="btnAdd_Click"/>
        </StackPanel>

        <DataGrid CanUserReorderColumns="False" Grid.Row="2" x:Name="dgAccount" AutoGenerateColumns="False"
                  IsReadOnly="True" SelectionMode="Single" Background="White"
                  BorderBrush="#E5E7EB" GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F9FAFB" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding AccountId}" Width="60"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="160"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="100"/>
                <DataGridTemplateColumn Header="Trạng thái" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock FontWeight="SemiBold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                <Setter Property="Text" Value="Hoạt động"/>
                                                <Setter Property="Foreground" Value="#10B981"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                <Setter Property="Text" Value="Đã khóa"/>
                                                <Setter Property="Foreground" Value="#EF4444"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Thao tác" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Sửa" Width="60" Height="26" Margin="0,0,6,0"
                                        Background="#F59E0B" Foreground="White" BorderThickness="0"
                                        FontSize="12" Cursor="Hand" Click="btnEdit_Click" Tag="{Binding AccountId}"/>
                                <Button Content="Xóa" Width="60" Height="26"
                                        Background="#EF4444" Foreground="White" BorderThickness="0"
                                        FontSize="12" Cursor="Hand" Click="btnDelete_Click" Tag="{Binding AccountId}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
